@model VeterinarskaStanica.Areas.ModulDoktor.Models.Vlasnici

<h2>Brisanje</h2>
<form asp-area="ModulDoktor" asp-controller="Vlasnici" asp-action="Delete" data-ajax="true" data-ajax-method="post" data-ajax-update="#ajaxpozivNalozi">
    
    @if (TempData["Message"] != null)
    {
    <div>
        <h3>@TempData["Message"]</h3>
    </div>
    }
    @Html.AntiForgeryToken()


    <div>
        <h4>Vlasnici</h4>
        <hr />
        <div class="form-group">

            <div class="col-md-10">
                @Html.DropDownListFor(model => model.Id, Model.listaVlasnika.Select(model => new SelectListItem
           {
               Value = model.Id.ToString(),
               Text = model.Ime + " " + model.Prezime
           }).ToList(), null, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>

        <div class="form-actions no-color">
            <input type="submit" value="Obriši" class="btn btn-default" />

        </div>
    </div>
    </form>